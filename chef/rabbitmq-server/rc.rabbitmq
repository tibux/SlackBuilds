#!/bin/bash

case "$1" in
  start)
    /usr/bin/rabbitmq-server -detached
    ;;
  stop)
    /usr/bin/rabbitmqctl stop
    ;;
  restart)
    $0 stop
    sleep 3
    $0 start
    ;;
  *)
    echo "usage: $0 {start|stop|restart}"
esac

